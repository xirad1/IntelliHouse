# Template for xwiki/xwiki.yml — copy to .env in this directory and fill in real values.
# Only used when running via plain `docker compose` (SSH + CLI); not used by QNAP Container Station's
# "Application" wizard — see CLAUDE.md ("How ${VAR} placeholders get real values per environment").
XWIKI_DB_ROOT_PASSWORD=xwiki
XWIKI_DB_PASSWORD=xwiki
